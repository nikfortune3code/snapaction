package com.snapaction

import android.app.Application

class SnapActionApp : Application() {
    override fun onCreate() {
        super.onCreate()
        // Initialize global components / Gemini Vision SDK / Supabase client if needed
    }
}
